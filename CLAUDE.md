# Claude Session Prompt — PlaneShift

> **State and backlog live in exactly two files.** Current state: `PROGRESS.md`. Backlog:
> `docs/MISSING_MECHANICS.md` (103 items, tagged [new]/[extend]/[fix]). Playtest instance and mod
> rules: `docs/PLAYTEST_INSTANCE.md`. This file carries *rules only* — do not restate state here,
> because four copies of it means three that are wrong. Before you finish a session, follow the
> **Handoff sync** section of `AGENTS.md`.



Copy-paste this into a new Claude session (Claude.ai, Claude Code, or Claude in Antigravity).

---

You are continuing the PlaneShift project. Be ultra-concise. No filler, no pleasantries, no repeating my prompt.

Repository: https://github.com/OkVurb/hand-off
Local root: C:\Dev\PlaneShift

## What this is
NeoForge 1.21.11 Minecraft mod (Java 21, NeoForge 21.11.45, MDG 2.0.144). Mario-inspired 2.5D platformer inside Minecraft — perspective-rail camera, Forms, enemies, courses, shift gates, checkpoints.

## Start here (in order)
1. `git pull` — get latest from main
2. Read **PROGRESS.md** — tells you exactly where the last session stopped
3. Read **AGENTS.md** — build invariants, conventions, jar locations. Its **Token budget**
   section is mandatory: The user pays per token and every tool result is re-sent with the whole conversation. Filter command output, batch edits before verifying, escalate compileJava -> test -> build rather than running a full build after every change, and read line ranges instead of whole files. Check whether a task is already done before implementing it.
4. Read **HANDOFF.md** sections 3.-6 through 3.0 — what was recently changed
5. `.\gradlew build` — verify green (only this-escape warnings expected)
6. If you touched data-pack files: `.\gradlew runServer` — verify zero ERROR lines

## Current state (as of last session)
- Build: GREEN. 38 JUnit tests pass. 5 Gradle check tasks pass.
- 5 generated courses (grass, desert, snow, lava, underground) with terrain, gaps, platforms, enemies, checkpoints, switches, shopkeeper, finish.
- Articulated enemy rig with 12 visual profiles, procedural animations, Blockbench workflow documented.
- Custom skybox, 6 course terrain blocks, 22 OGG sound files, 96+ placeholder textures.
- CI runs `./gradlew build` on every push.
- All P0/P1/P2 issues from original handoff are CLOSED.

## Build invariants (enforced by Gradle — read AGENTS.md before bypassing)
1. **checkClientClassLeak** — common/server must never reference net.minecraft.client
2. **checkNoRawCuboidScan** — area block searches go through BlockAreaScan, not BlockPos.betweenClosed
3. **checkSoundAssets** — every ModSounds entry has a real OGG and subtitle key
4. **checkTextureAssets** — every registered block/entity/effect/particle has a texture, no two identical
5. **checkBlockModels** — model/blockstate JSON parses and resolves; multi-variant blocks must differ per state; items need assets/planeshift/items/<id>.json; pack.mcmeta must use min_format/max_format with major > 81

## What the build CANNOT catch
Data-pack schema errors parse as valid JSON but crash at codec load time. After touching src/main/resources/data/, ALWAYS run `.\gradlew runServer` and check for "Done (Xs)!" with zero ERROR lines.

## Remaining work (priority order)
1. **Play-testing** — human needed. `.\gradlew runClient`, then `/planeshift role planeshift:balanced`, `/planeshift course course_1`. Walk all 5 courses: A/D movement, coyote time, glider, shift gates, checkpoints, death below kill_y, Toad shop, `/planeshift leave`.
2. **GameTests** — zero exist. build.gradle configures gameTestServer. 1.21.11 uses data-driven test_instance registry; NeoForge bridges via RegisterGameTestsEvent (mod bus). Start with QuestionBlock, PSwitchBlock, OnOffSwitch.
3. **JUnit coverage** — only PlaneConstrainedInputTest (31 cases) + CourseLayoutPlanTest (7 cases). Add tests for FormService, CourseService, CheckpointService.
4. **Bespoke entity models** — use Blockbench + turnaround sheets in tools/art_sources/ + docs/ENEMY_ART_DIRECTION.md. Current rig is shared/procedural.
5. **Real art and audio** — replace placeholder PNGs and synthesized OGGs at same paths. See ASSET_LICENSES.md.
6. **Smaller issues:**
   - src/generated/resources/ is empty but build.gradle claims datagen output is checked in. Run runClientData and commit, or fix the comment.
   - ModEffects: ice_aura and mini_aura share colour 0x88CCFF. Registry-level fix.
   - this-escape warnings in block constructors. Cosmetic.

## Jar/cache locations (generated by build, NOT in git)
- NeoForge jars: `build/moddev/artifacts/` (merged, sources, coremod)
- Built mod jar: `build/libs/planeshift-0.1.0.jar`
- Decompiled sources: `.mcsources/` (gitignored, regenerated)
- Gradle deps: `C:\Users\<user>\.gradle\caches\`
- Test runtime: `run/`

## API lookup
When unsure of a Minecraft/NeoForge API, read decompiled source in `.mcsources/` or extract `build/moddev/artifacts/neoforge-21.11.45-sources.jar`. The 1.21.11 schemas for dimension types, pack metadata, and item models changed recently — guessing costs more than looking.

## Rules
- Do not commit build/, .gradle/, run/, .mcsources/, or secrets.
- Prefer existing patterns. Codebase favours small focused Gradle check tasks over broad frameworks.
- Before changing event wiring, verify mod bus vs game bus.
- Verify any new build check by deliberately breaking what it guards.
- **Before ending your session, update PROGRESS.md** with: what you did, what's in progress, what's next, build status. This is mandatory — it's how the next session knows where you stopped.

## Goal
Pick one item from "Remaining work", implement it, run `.\gradlew build` AND `.\gradlew runServer` (if data touched), update PROGRESS.md, commit and push.
