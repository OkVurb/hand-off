# Next Claude Session Prompt — PlaneShift

Copy and paste the block below into a fresh Claude/ChatGPT/Devin session to continue from here.

---

```
You are continuing the PlaneShift project.

Repository: https://github.com/OkVurb/hand-off
Local root (if already cloned): C:\Dev\PlaneShift

High-level context:
- PlaneShift is a NeoForge 1.21.11 Minecraft mod (MDG 2.0.144, Java 21, NeoForge 21.11.45).
- Architecture: common/ (registries, blocks, entities, items, course/mode/rail state, Form framework, networking), server/ (authoritative services), client/ (keybinds, HUD, camera, input, music, renderers, screens).
- Client code must stay in com.studio.planeshift.client. Run checkClientClassLeak to verify.

Start here:
1. Clone or pull the repo (git clone https://github.com/OkVurb/hand-off.git).
2. Verify the environment: Java 21 JDK installed, Gradle wrapper present (gradlew).
3. Run a full build: .\gradlew build (Windows) or ./gradlew build (Linux/macOS). It should pass with only this-escape warnings.
4. Read HANDOFF.md first. It has the current state, what was changed, remaining issues, and the full file inventory.
5. Read AGENTS.md for project notes and build commands.
6. Read MCP_TOOLS.md for the list of available MCP tools.

Accessing Minecraft/NeoForge APIs for coding:
- After the first build, these artifacts are generated under build/moddev/artifacts/:
  - neoforge-21.11.45-merged.jar (merged client+server classes)
  - neoforge-21.11.45-sources.jar (decompiled sources)
  - neoforge-21.11.45.jar (NeoForge classes)
- .mcsources/ is a cache of extracted decompiled sources for quick API lookups. It is generated by the build and is in .gitignore.
- Use javap on the merged jar, extract sources from neoforge-21.11.45-sources.jar, or use the minecraft-modding / mcdev MCP tools to inspect classes and methods.
- MCP tools of interest: inspect-minecraft, analyze-symbol, mc_get_class, mc_get_method, mc_search, mc_find_refs.

Current priority work (from HANDOFF.md section 4):
1. P0 — Sound assets: assets/planeshift/sounds.json lists empty sound arrays and there are no .ogg files under assets/planeshift/sounds/. All audio is silent. Add real .ogg files and update sounds.json.
2. P1 — Client-side movement: PlaneConstrainedInput directly mutates LocalPlayer motion/rotation and may cause rubber-banding. Refactor it to only modify Input/moveVector, not LocalPlayer motion.
3. P1 — Brute-force block scans: OnOffSwitchBlock and PSwitchBlock scan large BlockPos ranges on every activation. Optimize with chunk or tag-based lookups.
4. P2 — Missing mob effect icons: add PNGs under assets/planeshift/textures/mob_effect/.
5. P2 — State-aware block models: on_off_block and hidden_question_block need distinct models for their states.
6. P2 — Coarse height checks: HiddenQuestionBlock, QuestionBlock, PrizeCacheBlock, BrickBlock should use bounding-box/head contact instead of whole-player Y.
7. P2 — C2S payload rate limiting: add per-player rate limits to CourseService.loadCourse and ToadShopService.purchase.

Build/verify commands:
- .\gradlew build
- .\gradlew compileJava
- .\gradlew checkClientClassLeak
- .\gradlew runClient
- .\gradlew runServer

Rules:
- Do not commit build/, .gradle/, run/, .mcsources/, or secrets.
- Use .gitignore and AGENTS.md as references.
- Prefer existing patterns and conventions in the codebase.
- Before changing client/server event wiring, verify which event bus (mod vs game) an event belongs to.
- Use the Devin skill or MCP tools to inspect Minecraft/NeoForge classes when unsure of method signatures.

Goal for this session: pick the highest-priority issue you can realistically finish, write a failing test or build check if possible, fix it, run .\gradlew build, and update HANDOFF.md with what you did.
```

---

## Where the jars/caches live on disk

These are generated by the build and are intentionally **not** in Git. They reappear after `gradlew build`:

- `build/moddev/artifacts/` — NeoForge merged jar, sources jar, and coremod jars used for API inspection.
- `build/libs/` — the built mod jar (`planeshift-0.1.0.jar`).
- `C:\Users\<user>\.gradle\caches\` — downloaded Gradle/Maven dependency jars.
- `.mcsources/` — decompiled vanilla/NeoForge source cache.
- `run/` — MDG test client/server directory.

To use them, just build once. To look up a class, inspect `build/moddev/artifacts/neoforge-21.11.45-merged.jar` or extract `neoforge-21.11.45-sources.jar`.
