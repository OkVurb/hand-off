package com.studio.planeshift.client.render;

/** Independently modelled visual archetypes for PlaneShift projectile entities. */
public enum ProjectileVisualProfile {
    EMBER_BOLT,
    HAMMER,
    FIREBALL,
    ICEBALL,
    BOOMERANG,
    BOWSER_FIRE,
    /** The grinder blade: a toothed disc on a hub. */
    GRINDER,
    /** The spiked ball on the end of a chain. */
    SPIKED_BALL,
    /** A lump of rock thrown out of an eruption. */
    FIRE_ROCK,
    /** The Koopalings' clown car, hanging over the final castle's approach. */
    CLOWN_CAR
}
